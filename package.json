{
  "name": "oval",
  "private": true,
  "engines": {
    "node": ">=22 <23",
    "npm": ">=10 <12"
  },
  "scripts": {
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "audit:a11y": "npm run audit:a11y --prefix frontend && npm run audit:a11y --prefix landing",
    "repo:check": "node scripts/release-check.mjs",
    "migrations:check": "node scripts/verify-migrations.mjs",
    "build": "npm run build --prefix backend && npm run typecheck --prefix frontend && npm run build --prefix landing",
    "doctor": "npm run doctor --prefix frontend",
    "export:ios": "npm run export:ios --prefix frontend",
    "audit:production": "node scripts/audit-production.mjs",
    "release:check": "npm run repo:check && npm run migrations:check && npm run audit:a11y && npm run test && npm run build && npm run doctor && npm run export:ios && npm run audit:production"
  }
}
